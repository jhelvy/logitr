% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{loglik_draws}
\alias{loglik_draws}
\title{Data frame of mixed logit log-likelihoods as the number of draws grows}
\format{
\tabular{ll}{
   Variable \tab Description \cr
   \code{numDraws} \tab The number of random draws used during estimation. \cr
   \code{seed} \tab The random seed used to generate the MLHS draws. \cr
   \code{logLik} \tab The converged log-likelihood of the model. \cr
   \code{time_sec} \tab The estimation time in seconds (on a 10-core Apple M-series Mac). \cr
}
}
\source{
The \code{data-raw/runtimes.R} script in the \href{https://github.com/jhelvy/logitr}{logitr repository}.
}
\usage{
data(loglik_draws)
}
\description{
This data frame contains the converged log-likelihoods of the same
preference space mixed logit model used in the \link{runtimes} and
\link{runtimes_draws} benchmarks, estimated repeatedly at increasing numbers of
random draws. At each draw count the model is estimated 10 times using
randomized MLHS draws with a different seed each time, with every run
starting from the same converged reference solution, so the across-seed
spread in log-likelihood at a given draw count reflects simulation error
and nothing else. It is used to show that low draw counts produce unstable
simulated log-likelihoods, which stabilize as the number of draws grows.
It is generated by the script \code{data-raw/runtimes.R} in the package
repository.
}
\examples{
data(loglik_draws)

head(loglik_draws)
}
\keyword{datasets}
