% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{runtimes}
\alias{runtimes}
\title{Data frame of run times for logitr benchmark}
\format{
\tabular{ll}{
   Variable \tab Description \cr
   \code{package} \tab Package name (with the number of cores in parentheses for packages benchmarked in parallel). \cr
   \code{time_sec} \tab The estimation time in seconds. \cr
   \code{numDraws} \tab The number of random draws used during estimation. \cr
   \code{version} \tab The installed version of the package used for the benchmark. \cr
}
}
\source{
The \code{data-raw/runtimes.R} script in the \href{https://github.com/jhelvy/logitr}{logitr repository}.
}
\usage{
data(runtimes)
}
\description{
This data frame contains the run times for a benchmark comparing the
relative computation time to estimate a preference space mixed logit model
using the following R packages: logitr, mixl, mlogit, gmnl, and apollo.
Packages that support parallel estimation (logitr, mixl, and apollo) are
benchmarked at multiple core counts, indicated in parentheses in the
\code{package} name (e.g. \code{"logitr (1 cores)"}). The benchmark is generated by the
script \code{data-raw/runtimes.R} in the package repository. The shipped results
were produced on a 10-core Apple M-series Mac, so the absolute times (and
the core counts benchmarked) are specific to that machine.
}
\examples{
data(runtimes)

head(runtimes)
}
\keyword{datasets}
